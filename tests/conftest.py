"""Shared pytest fixtures for the wodbuster_worker test suite.

Currently empty. Component and contract layers will populate this with
SQLite, httpx mock, and APScheduler fixtures in later phases.
"""
